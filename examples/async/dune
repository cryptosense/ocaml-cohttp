(executables
  (names     hello_world receive_post)
  (libraries
    async_kernel
    cohttp-async
    base
    cstruct
    nocrypto))

(alias
  (name    runtest)
  (package cohttp-async)
  (deps    hello_world.exe receive_post.exe))
